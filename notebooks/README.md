# Notebooks

Tutorial notebooks live in `notebooks/tutorials/`.

```text
tutorials/
  authority.ipynb    # load and inspect authority_data/data/authority
  benchmarks.ipynb   # load and inspect authority_data/data/benchmarks
```

Install notebook dependencies from the repository root:

```bash
pip install -e ".[notebooks]"
```
